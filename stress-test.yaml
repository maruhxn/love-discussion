config:
  target: "http://localhost:8080"
  phases:
    - duration: 60
      arrivalRate: 1
      name: Warm Up
scenarios:
  - name: "GET all chats"
    flow:
      - get:
          url: "/api/v1/chats"
          count: 1000 # Number of requests per virtual user
          method: GET
